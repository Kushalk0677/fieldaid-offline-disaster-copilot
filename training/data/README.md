# `training/data/`

Small seed manifests and examples for FieldAid training experiments.

This folder is for lightweight, publishable training inputs such as JSONL examples and manifest templates. Large downloaded image datasets should stay in `modeltraining/` locally and should not be committed.
